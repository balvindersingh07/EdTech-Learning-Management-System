# Screenshot checklist — testing proof

- [ ] **Local `npm test`** — full Vitest output, all tests passing.
- [ ] **Coverage (optional)** — if you add `vitest --coverage`, capture HTML or terminal summary.  
  Placeholder: *“Attach `coverage/lcov-report/index.html` screenshot or terminal summary.”*
- [ ] **Single test file focus** — e.g. `gradingStrategy.test.js` run to show Strategy tested without Express.

**Commands for reports:**

```bash
npm test
```

Save as `docs/screenshots/testing-vitest.png`.
